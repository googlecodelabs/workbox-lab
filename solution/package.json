{
  "name": "workbox-lab",
  "version": "1.0.0",
  "description": "a lab for learning workbox",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "copyfiles -u 1 app/**/**/* app/**/* app/* build && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "workbox-sw": "^2.0.0"
  },
  "devDependencies": {
    "copyfiles": "^1.2.0"
  }
}
